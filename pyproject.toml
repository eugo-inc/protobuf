[build-system]
build-backend = 'mesonpy'
requires = ['meson-python']

[project]
name = 'protobuf'
readme = 'README.md'
requires-python = '>=3.12'

# Must match `meson.build#project#version`
version = '5.30.0'
